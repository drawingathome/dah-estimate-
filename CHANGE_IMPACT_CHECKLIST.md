# 데이터/인증/권한 변경 시 필수 체크리스트

이 문서는 2026-07-15 세션에서 RLS(권한잠금) 도입 중 반복적으로 버그를
놓쳤던 경험을 바탕으로 만들어졌다. **로그인, 권한, 데이터 저장구조,
삭제/복구처럼 여러 화면에 걸쳐 영향을 주는 변경**을 할 때는, 코드를
고치기 전에 반드시 이 체크리스트를 먼저 확인한다.

## 1. 전수조사 먼저, 코드는 나중에

변경을 시작하기 전에:
```
grep -rn "관련 API/필드/함수명" *.js
```
로 **관련된 접점을 전부 나열**하고, 표로 정리한다. "몇 군데 정도 있겠지"
라는 감으로 시작하지 않는다. (실제로 오늘 Supabase 접근 지점은
"몇 군데"가 아니라 정확히 12곳이었다.)

## 2. 이름을 새로 짓기 전에 충돌 검사

새 필드명/함수명을 코드에 추가하기 전, **그 이름이 이미 다른 의미로
쓰이고 있지 않은지 먼저 검색**한다.
```
grep -rn "isArchived\b" *.js   # 예: 새로 쓰려는 이름
```
이미 있다면 절대 재사용하지 말고 새 이름을 쓴다. (오늘
`is_archived`/`isArchived`가 "완료 후 14일 경과 자동보관"이라는
전혀 다른 기존 기능과 충돌해서, 복구 버튼이 계속 안 뜨는 버그로 이어졌다.)

## 3. "이 데이터를 보여주는 화면이 몇 개인가"부터 나열

고객/견적 데이터처럼 여러 화면(홈, 칸반, 캘린더, 검색, 매출탭, 엑셀
내보내기, 문서생성)에 걸쳐 쓰이는 데이터의 상태를 바꿀 때는, **그
데이터를 렌더링하는 모든 함수 목록을 먼저 뽑는다**:
```
grep -rn "function render" *.js
```
그 다음 하나씩 "이 필터가 여기에도 적용됐는가"를 확인한다. 하나
고치고 끝내지 않는다. (오늘 삭제 필터를 renderSearch에만 넣고,
renderHome/renderPipeKanban/renderCal/renderChart/exportExcel
5곳에는 빠뜨렸다가 나중에 따로따로 발견했다.)

## 4. 만든 함수가 실제로 호출되는지 확인

함수를 정의하는 것과, 그 함수가 실행되는 코드 경로가 있는 것은
다른 문제다. 특히 "자동으로 주기적으로 실행돼야 하는" 함수는:
```
grep -rn "함수이름(" *.js | grep -v "function 함수이름"
```
로 실제 호출부가 있는지 반드시 확인한다. (오늘
`refreshAuthSessionIfNeeded`를 만들어놓고 아무데서도 안 불러서,
로그인 1시간 후 데이터가 조용히 로컬에만 저장되는 위험이 있었다.)

## 5. 권한/보안을 잠글 때는 "지금 뭐가 이걸 쓰고 있나"부터

RLS, 로그인 요구사항처럼 접근을 제한하는 변경을 하기 전에:
- 이 테이블/API를 사용하는 모든 클라이언트(웹앱, Google Apps Script,
  외부에서 오는 공개 폼 제출 등)를 전부 나열
- 그 중 **"로그인 없이 접근해야 하는 것"**(예: 고객이 직접 작성하는
  공개 설문지)과 **"서버에서만 도는 스크립트"**(관리자 키 필요)를
  구분해서 표로 정리한 다음에 잠근다
- "일단 다 잠그고 문제 생기면 그때 열자"가 아니라, 잠그기 전에
  이 표를 완성한다

## 6. 버그를 고쳤으면 반드시 회귀테스트로 고정

버그를 찾아서 고치는 것으로 끝내지 않는다. `tests/` 폴더에 그
버그를 재현하는 자동 테스트를 추가하고, **일부러 수정 전 코드로
되돌려서 테스트가 실제로 실패하는지 확인한 뒤** 다시 복구하고 배포한다.
이게 없으면 같은 버그가 다음 리팩토링 때 조용히 재발한다.

## 7. 배포 전 마지막 질문

"이걸 배포하면 지금 정상 작동하는 기존 기능 중 뭐가 깨질 수 있는가?"
를 스스로에게 묻고, 답이 안 나오면 그건 충분히 검토 안 했다는
신호다. "아마 괜찮을 것"이라는 느낌은 근거가 아니다.
