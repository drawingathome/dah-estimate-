# 데이터/인증/권한 변경 시 필수 체크리스트

이 문서는 2026-07-15 세션에서 RLS(권한잠금) 도입 중 반복적으로 버그를
놓쳤던 경험을 바탕으로 만들어졌다. **로그인, 권한, 데이터 저장구조,
삭제/복구처럼 여러 화면에 걸쳐 영향을 주는 변경**을 할 때는, 코드를
고치기 전에 반드시 이 체크리스트를 먼저 확인한다.

## 1. 전수조사 먼저, 코드는 나중에

변경을 시작하기 전에:
```
grep -rn "관련 API/필드/함수명" *.js
```
로 **관련된 접점을 전부 나열**하고, 표로 정리한다. "몇 군데 정도 있겠지"
라는 감으로 시작하지 않는다. (실제로 오늘 Supabase 접근 지점은
"몇 군데"가 아니라 정확히 12곳이었다.)

## 2. 이름을 새로 짓기 전에 충돌 검사

새 필드명/함수명을 코드에 추가하기 전, **그 이름이 이미 다른 의미로
쓰이고 있지 않은지 먼저 검색**한다.
```
grep -rn "isArchived\b" *.js   # 예: 새로 쓰려는 이름
```
이미 있다면 절대 재사용하지 말고 새 이름을 쓴다. (오늘
`is_archived`/`isArchived`가 "완료 후 14일 경과 자동보관"이라는
전혀 다른 기존 기능과 충돌해서, 복구 버튼이 계속 안 뜨는 버그로 이어졌다.)

## 3. "이 데이터를 보여주는 화면이 몇 개인가"부터 나열

고객/견적 데이터처럼 여러 화면(홈, 칸반, 캘린더, 검색, 매출탭, 엑셀
내보내기, 문서생성)에 걸쳐 쓰이는 데이터의 상태를 바꿀 때는, **그
데이터를 렌더링하는 모든 함수 목록을 먼저 뽑는다**:
```
grep -rn "function render" *.js
```
그 다음 하나씩 "이 필터가 여기에도 적용됐는가"를 확인한다. 하나
고치고 끝내지 않는다. (오늘 삭제 필터를 renderSearch에만 넣고,
renderHome/renderPipeKanban/renderCal/renderChart/exportExcel
5곳에는 빠뜨렸다가 나중에 따로따로 발견했다.)

## 4. 만든 함수가 실제로 호출되는지 확인

함수를 정의하는 것과, 그 함수가 실행되는 코드 경로가 있는 것은
다른 문제다. 특히 "자동으로 주기적으로 실행돼야 하는" 함수는:
```
grep -rn "함수이름(" *.js | grep -v "function 함수이름"
```
로 실제 호출부가 있는지 반드시 확인한다. (오늘
`refreshAuthSessionIfNeeded`를 만들어놓고 아무데서도 안 불러서,
로그인 1시간 후 데이터가 조용히 로컬에만 저장되는 위험이 있었다.)

## 5. 권한/보안을 잠글 때는 "지금 뭐가 이걸 쓰고 있나"부터

RLS, 로그인 요구사항처럼 접근을 제한하는 변경을 하기 전에:
- 이 테이블/API를 사용하는 모든 클라이언트(웹앱, Google Apps Script,
  외부에서 오는 공개 폼 제출 등)를 전부 나열
- 그 중 **"로그인 없이 접근해야 하는 것"**(예: 고객이 직접 작성하는
  공개 설문지)과 **"서버에서만 도는 스크립트"**(관리자 키 필요)를
  구분해서 표로 정리한 다음에 잠근다
- "일단 다 잠그고 문제 생기면 그때 열자"가 아니라, 잠그기 전에
  이 표를 완성한다

## 6. 버그를 고쳤으면 반드시 회귀테스트로 고정

버그를 찾아서 고치는 것으로 끝내지 않는다. `tests/` 폴더에 그
버그를 재현하는 자동 테스트를 추가하고, **일부러 수정 전 코드로
되돌려서 테스트가 실제로 실패하는지 확인한 뒤** 다시 복구하고 배포한다.
이게 없으면 같은 버그가 다음 리팩토링 때 조용히 재발한다.

## 7. 배포 전 마지막 질문

"이걸 배포하면 지금 정상 작동하는 기존 기능 중 뭐가 깨질 수 있는가?"
를 스스로에게 묻고, 답이 안 나오면 그건 충분히 검토 안 했다는
신호다. "아마 괜찮을 것"이라는 느낌은 근거가 아니다.

## 8. "한 번 고친 버그"는 같은 패턴이 다른 곳에 더 있는지 항상 재검사

같은 위험한 코드 패턴(예: "로컬 저장 직후 서버 재조회로 덮어쓰기")은
여러 파일에 복사되어 있을 수 있다. 한 곳을 고쳤다고 끝난 게 아니다.

- 버그를 고치면, **그 패턴 자체를 grep으로 코드베이스 전체에서
  재검색**한다. (2026-07-16: `saveCustomers(` 직후 `loadCustomersAsync(`를
  호출하는 패턴이 서로 다른 파일 3곳(대시보드 고객상세, 칸반)에
  복사되어 있었고, 하나만 고치고 넘어갔다가 재검토 요청으로
  나머지를 뒤늦게 발견함)
- 가능하면 **자동 정적분석 스크립트**를 만들어 `tests/`에 추가하고
  `run-all.js`에 연결한다. 사람이 "이 패턴 또 있나?"를 기억해서
  매번 확인하는 것보다, 기계가 매 배포 전에 자동으로 검사하는 게
  훨씬 안전하다. (`tests/anti-pattern-check.js` 참고 — 이 파일
  자체가 그 사례)
- "완전히 고쳐졌는지 확인해달라"는 요청을 받으면, 방금 고친 곳만
  재확인하지 말고 **같은 유형의 코드가 다른 파일에도 없는지**부터
  다시 훑는다.

## 9. 기존 화면에 새 UI 섹션을 추가하기 전, 이미 있는 섹션들과 목적이 겹치는지 확인

새 카드/목록을 화면에 추가하기 전에, **그 화면에 이미 있는 다른 카드/목록들이
어떤 조건으로 어떤 데이터를 보여주는지 먼저 나열**한다. 안 그러면 같은
데이터가 서로 다른 이유로 두 카드에 겹쳐서 나타나는 걸 눈치 못 채고 배포하게 된다.

- (2026-07-16: 홈화면에 "발주 시작 안 된 고객" 카드를 새로 추가했는데,
  이미 있던 "처리 필요" 카드와 조건이 겹쳐서 같은 고객 이름이 두 카드에
  중복으로 나타났다. "한눈에 보이는지 확인해달라"는 요청을 받고서야
  실제 화면을 출력해보고 발견함 — 코드는 각각 문법적으로 멀쩡했기 때문에
  일반적인 버그 테스트로는 안 잡혔다.)
- 여러 조건에 동시에 해당할 수 있는 테스트 데이터를 일부러 만들어서,
  **같은 이름이 화면 전체에 몇 번 나타나는지 세어보는 회귀테스트**를 추가한다
  (`tests/home-duplication-check.js` 참고). 코드가 "문법적으로 맞다"와
  "실제 화면이 안 겹친다"는 다른 문제이므로, 반드시 렌더링된 텍스트를
  세어서 검증해야 한다.
- 가능하면 여러 조합의 실제 데이터로 화면을 렌더링해 그 결과(텍스트든
  스크린샷이든)를 실제로 살펴보는 단계를 새 UI를 추가할 때마다 거친다.
  "문법 검사 통과"나 "각 기능 개별 테스트 통과"만으로는 이런 종류의
  중복/혼란을 못 잡는다.

## 10. "이 값이 정말 변하지 않는 고유번호인가?"를 항상 의심하기

어떤 값을 "이 레코드가 그 레코드다"를 판단하는 기준(매칭 키)으로 쓰기 전에,
**그 값이 정말로 매번 저장해도 안 바뀌는지** 반드시 확인한다.

- (2026-07-16: 견적서를 저장할 때마다 "고객을 찾는 기준"으로 견적서 번호를
  썼는데, 이 번호는 저장할 때마다 달라질 수 있는 값이었다. 그 결과 같은
  고객이 여러 번 저장할 때마다(가견적→확정견적 등) 매번 새 고객 레코드가
  로컬과 서버 양쪽에 계속 쌓였다. 겉으로는 "동명이인 데이터가 섞여
  보이는" 증상으로 나타나서 원인 파악이 오래 걸렸다.)
- **의심 신호**: 매칭 키로 쓰려는 값이 (1) 사용자가 매번 입력/변경할 수
  있는 값이거나, (2) 저장할 때마다 새로 생성되는 값(주문번호, 견적번호,
  타임스탬프 등)이면 위험하다. 진짜 안전한 매칭 키는 데이터베이스가
  발급한 고유번호(id/UUID)뿐이다.
- **`id`라는 변수명을 쓴다고 안전해지는 게 아니다.** 오늘 사고도 변수명이
  정확히 `id`였지만, 실제 값은 "견적서 번호"였다 — 이름이 아니라 "이
  값의 실제 출처가 데이터베이스가 발급한 값인가"를 확인해야 한다.
- 새로운 저장/매칭 로직을 작성할 때: "이 값을 두 번 저장하면 똑같은
  값이 나오는가?"를 스스로에게 묻는다. 아니라면 그 값을 매칭 키로
  쓰면 안 된다.
## 11. 쓰기(저장/수정/삭제) 작업은 "성공 응답"이 아니라 "다시 조회해서" 검증하기

서버가 200이나 204(성공) 응답을 줬다고 해서 실제로 반영됐다고 믿으면 안 된다.

- (2026-07-17: DELETE 요청이 계속 "성공"으로 응답이 왔는데, 실제로는
  이 프로젝트의 보안 정책(RLS)이 삭제 자체를 막고 있어서 아무것도
  지워지지 않고 있었다. 이걸 몇 시간 동안 못 알아챈 이유는, 매번
  "삭제됐습니다"라는 로그만 보고 넘어갔지, 삭제 후 다시 조회해서
  "진짜 없어졌는지"를 확인한 적이 없었기 때문이다.)
- **원칙**: 데이터를 바꾸는 요청(POST/PATCH/DELETE)을 보낸 뒤에는,
  가능하면 항상 같은 조건으로 다시 조회(GET)해서 실제로 값이 바뀌었는지
  눈으로 확인하는 단계를 포함시킨다. 특히 처음 만드는 삭제/정리 로직은
  반드시 이 검증 단계를 넣는다.
- Supabase(PostgREST)에서 DELETE/UPDATE가 0건을 지워도/바꿔도 에러
  없이 200번대를 반환한다는 것도 함께 기억한다 — "응답 코드가 성공"과
  "실제로 몇 건 처리됐는지"는 별개의 정보다.

## 12. 가짜 서버로 통과한 테스트만으로는 부족하다 — 진짜 데이터베이스로도 검증

오늘 세션 대부분의 자동 테스트는 실제 Supabase 대신 흉내낸(mock) 응답을
쓰는 방식이었다. 이 방식은 "코드가 올바른 요청을 만드는지"는 검증하지만,
"그 요청을 진짜 데이터베이스가 받아줄 수 있는지"(테이블에 그 컬럼이
진짜 있는지, 트리거가 막고 있진 않은지, 삭제 권한이 있는지)는 절대 못
잡는다.

- (2026-07-17: `customers` 테이블에 트리거가 기대하는 `updated_at`
  컬럼이 없어서 모든 수정이 실패하고 있었고, `estimates` 테이블은
  코드가 보내는 필드 이름과 실제 컬럼 구조 자체가 달랐다. 둘 다
  가짜 서버 테스트는 전부 통과했지만, 실제 서비스에서는 하루 종일
  저장이 안 되고 있었을 수 있는 문제였다.)
- **원칙**: 데이터 저장/구조와 관련된 큰 변경 이후에는, 안전하게 설계된
  진단 도구(임시 테스트 레코드를 실제로 만들었다가 곧바로 정리하는
  방식)로 **진짜 데이터베이스에 대고 한 번은 검증**한다. 이 프로젝트는
  Google Apps Script를 통해서만 실제 서버에 접근 가능하므로, 이런
  진단 함수들을 앱스스크립트 쪽에 마련해두고 필요할 때 쓴다.

## 13. 외부(CDN) 스크립트는 반드시 defer/async — 안 그러면 그 서버 하나 때문에 앱 전체가 멈출 수 있음

`<script src="https://외부주소/...">`를 `defer` 없이 넣으면, 그 스크립트를
전부 받을 때까지 그 뒤의 모든 HTML 파싱과 스크립트 실행이 멈춘다.

- (2026-07-17: 실시간 동기화 기능을 위해 추가한 CDN 스크립트를 defer
  없이 맨 앞에 넣었는데, 그 CDN 접속이 느리거나 막히면 견적서 버튼을
  포함한 앱 전체 기능이 멈춰버리는 심각한 문제로 이어졌다.)
- **원칙**: 우리 서비스 자체 파일이 아닌 모든 외부 스크립트 태그에는
  예외 없이 `defer` 속성을 붙인다. 그 외부 서비스가 오늘 당장은 잘
  되더라도, 사용자의 네트워크 환경에 따라 언제든 느려지거나 막힐 수
  있다는 걸 기본 전제로 삼는다.

## 14. 파일을 여러 개로 쪼갠 뒤에는, 테스트/검증할 때 새로 생긴 파일도 함께 챙기기

코드를 여러 파일로 분리하는 리팩토링을 하면, 그 이후의 모든 검증
작업(로컬 재현 테스트, 디버깅용 임시 확인 등)에서 **새로 생긴 파일들도
빠짐없이 함께 불러와야** 한다.

- (2026-07-17: `dash-render.js`에서 `renderSearch`를 별도 파일
  `dash-render-search.js`로 분리하는 작업이 있었는데, 그 이후 다른
  작업을 검증할 때 이 새 파일을 깜빡하고 안 받아와서 "고객목록 화면이
  깨졌다"고 잘못 판단하고 놀랐던 적이 있다. 실제 배포본은 멀쩡했다.)
- **원칙**: 파일을 분리한 커밋이 있었다면, 그 이후의 모든 검증
  작업에서 최신 파일 목록을 다시 확인(`git ls-tree` 또는 저장소 전체
  목록 조회)하고 받아온다. "저번에 받았던 파일 목록"을 재사용하지 않는다.
